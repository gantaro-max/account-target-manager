# 取引先ターゲット管理アプリ - エージェント定義

プロジェクト概要は `docs/PROJECT_OVERVIEW.md` を参照してください。

---

## エージェント構成概要

本プロジェクトでは、以下の5エージェントが役割分担して機能します。  
指揮・調整はTech Lead（Claude / `CLAUDE.md`）が担います。

| エージェント | 役割 | 作業領域 |
|---|---|---|
| **Codex** | 実装・セルフテスト・一次レビュー | `src/` 配下のコードのみ |
| **Agent-A** | 一次・最終レビュー：指示書との整合性確認 | `docs/instructions/plan_*.md` ↔ `src/` |
| **Agent-B** | 一次・最終レビュー：データ整合性確認 | `src/` ↔ `DATABASE_DESIGN.md` |
| **Agent-C** | 一次・最終レビュー：UI/UX整合性確認 | `src/` ↔ `UI_FLOW.md` |
| **Agent-D** | 一次・最終レビュー：コード品質・セキュリティ確認 | `src/` |

**レビューは2ステージ制です。**

1. **一次レビュー（Codex）**: 実装後にCodexがAgent-A〜Dを**並列起動**して独立した視点で検証する。1件でもNGが出た場合は自己修正して全エージェントを再起動する。**全エージェントがOKを出してから**Claudeに報告する（AND条件）。
2. **最終レビュー（Claude）**: Claude がAgent-A〜Dを**並列起動**して独立した視点で再検証する。**全エージェントがOKを出した場合のみ**承認（AND条件）。

---

## Codex（実装エージェント）

### 役割

Tech Lead（Claude）が作成した `docs/instructions/plan_*.md` に完全に忠実に、高速かつバグのないコードを出力することが任務です。

### 作業領域の制限

- 作業対象は `src/` フォルダ内のGASサーバーサイドコード（`*.js`）およびフロントエンドコード（`*.html`）のみ。
- 仕様の決定やドキュメント（`docs/`）の直接的な書き換えは行わない。

### 厳守すべきアーキテクチャ・ルール

1. **データ構造とID管理**（`DATABASE_DESIGN.md` 準拠）  
   取引先（`h…`）・部署（`d…`）・案件（`p…`）・ログ（`l…`）のID紐付けと階層構造を正確に扱うこと。

2. **クラウド同期（一括Upsert）**  
   フロントエンド（メモリ上）の変更を最後に一括でバックエンド（スプレッドシート）へ送る方式を維持し、細かな都度通信は避けること（GASの実行制限対策）。

3. **カスケード削除の徹底**  
   上位階層（取引先・部署）の削除処理を実装する際は、必ず紐づく下位データ（案件・ログ・`stage_history`）も連鎖的に削除するロジックを含めること。

4. **GASの制限・UI体験**（`REQUIREMENTS.md` 準拠）  
   `google.script.run` による非同期通信中は、必ず `showOverlay()` / `hideOverlay()` でローディング状態をUIに表示し、ユーザー操作の競合を防ぐこと。

### 実装・一次レビューワークフロー

1. **[理解]**: 指定された `plan_*.md` を読み込み、要件と完了条件（DoD）を把握する。
2. **[コーディング]**: スケルトン（最小構成）のコードから始め、段階的に詳細なロジックを実装し、適用エラーを防ぐ。
3. **[セルフテスト]**: 実装したコードを自分でトレースし、主要な操作フローが要件通りに動くかを確認する。
4. **[一次レビュー]**: Agent-A〜Dを**同時に並列起動**し、それぞれの観点から独立したレビューを行わせる。1件でもNGが出た場合は指摘内容をもとに自己修正し、全エージェントを再起動する。**全エージェントがOKを出すまでこのサイクルを繰り返す。**
5. **[報告]**: 全エージェントがOKになったら、変更ファイル・修正内容・一次レビュー結果のサマリーをTech Lead（Claude）に報告し、最終レビューを待つ。

---

## Agent-A（指示書整合性レビュー）

### 担当観点

実装内容が `plan_*.md` の要件・完了条件をすべて満たしているかを確認する。

### 参照ドキュメント

- `docs/instructions/plan_*.md`（該当する指示書）

### チェック項目

- [ ] 指示書に記載された**変更対象ファイルがすべて変更**されているか
- [ ] 指示書の**機能要件の各項目**が実装されているか
- [ ] **完了条件（DoD）の全項目**が満たされているか
- [ ] 指示書に**記載のない変更**（スコープ外の改変）が含まれていないか

### 判定基準

- **OK**: DoD全項目が満たされており、スコープ外の変更がない
- **NG**: 1項目でも未達・逸脱がある → 差し戻し

---

## Agent-B（データ整合性レビュー）

### 担当観点

実装がプロジェクトのデータアーキテクチャルールを守っているかを確認する。

### 参照ドキュメント

- `docs/DATABASE_DESIGN.md`

### チェック項目

**ID生成ルール**
- [ ] 取引先ID: `h` + timestamp 形式で生成されているか
- [ ] 部署ID: `d` + timestamp 形式で生成されているか
- [ ] 案件ID: `p` + timestamp 形式で生成されているか
- [ ] ログID: `l` + timestamp 形式で生成されているか

**外部キー設定**
- [ ] `departments.accountId` に取引先IDが正しく設定されているか
- [ ] `projects.accountId` / `projects.departmentId` が正しく設定されているか
- [ ] `activity_logs.projectId` / `activity_logs.accountId` が正しく設定されているか

**カスケード削除**
- [ ] 取引先削除時: 部署・案件・ログ・`stage_history` が全件削除されているか
- [ ] 部署削除時: 案件・ログ・`stage_history` が全件削除されているか
- [ ] 案件削除時: ログ・`stage_history` が全件削除されているか

**Upsert同期**
- [ ] フロントエンドのメモリ変更が一括送信される方式を維持しているか（個別の都度通信が追加されていないか）

**ステージ変更履歴**
- [ ] ステージ変更時に `stage_history` へのレコード追加が行われているか

### 判定基準

- **OK**: 全チェック項目がクリアされている
- **NG**: 1件でも違反がある → 差し戻し

---

## Agent-C（UI/UX整合性レビュー）

### 担当観点

実装された画面・操作フローが `UI_FLOW.md` の定義と一致しているかを確認する。  
加えて、**`docs/PATTERNS.md`（実装パターン早見表）および `src/` の既存実装**と照合し、同種機能との実装パターン整合性を確認する。

### 参照ドキュメント

- `docs/UI_FLOW.md`
- `docs/PATTERNS.md`（実装パターン早見表）

### チェック項目

**権限制御**
- [ ] `manager` はログ入力フォーム・クラウド同期ボタン・取引先登録タブが非表示になっているか
- [ ] 取引先削除は `admin` のみ実行可能になっているか
- [ ] 担当者フィルタ（統計タブ）は `admin`/`manager` のみ表示されているか

**ローディング・非同期UX**
- [ ] GAS呼び出しの用途（通常操作 or CSVダウンロード）に応じて、`docs/PATTERNS.md` の正しいローディングパターンが使われているか
- [ ] 通信完了後（成功・失敗ともに）ローディングが必ず解除されているか

**ダイアログ**
- [ ] 削除確認は SweetAlert2 の「キャンセル / 削除する」2ボタン形式になっているか
- [ ] エラー通知は `Swal.fire("エラー", e.message, "error")` 形式になっているか
- [ ] 保存成功時に緑色のトースト通知が表示されるか

**案件カード表示**
- [ ] 優先度の左ボーダー色が `high=赤 / mid=アンバー / low=グレー` になっているか
- [ ] 期限超過時に背景色が薄赤（`#FEF2F2`）になり、⚠️アイコンが表示されるか

**クイック登録ウィザード**
- [ ] 取引先 → 部署 → 案件の連続登録フローが維持されているか

**既存実装との一貫性**
- [ ] 実装した機能と同種・同カテゴリの既存関数を `src/` から検索し、`docs/PATTERNS.md` の各項目（ローディング・エラー・ダウンロード・DOM操作・スタイル）が一致しているか

### 判定基準

- **OK**: `UI_FLOW.md` との乖離がなく、`docs/PATTERNS.md` および既存実装とのパターン不一致がない
- **NG**: 1件でも乖離・不一致がある → 差し戻し

---

## Agent-D（コード品質・セキュリティレビュー）

### 担当観点

セキュリティ脆弱性・GAS制限への配慮・コード可読性を確認する。

### チェック項目

**セキュリティ**
- [ ] `innerHTML` 等に未サニタイズのユーザー入力が埋め込まれていないか（XSS）
- [ ] `eval()` や動的コード実行が使われていないか

**GAS制限への配慮**
- [ ] 単一実行で6分制限を超えうる重いループ処理が含まれていないか
- [ ] `google.script.run` に `withSuccessHandler()` / `withFailureHandler()` が両方設定されているか

**コード品質**
- [ ] 意図が読めないマジックナンバー・未使用変数・デッドコードがないか
- [ ] 既存の命名規則・インデントスタイルと一貫しているか

### 判定基準

- **OK**: セキュリティ上のCritical問題がなく、品質上の重大な懸念がない
- **NG**: XSS・GAS実行エラーを引き起こす実装・デッドコード等が1件でもある → 差し戻し

---
