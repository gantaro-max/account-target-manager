# plan_YYYYMMDD_<機能名>

<!--
指示書の粒度の原則:
  実装AIが「判断」しなくても書ける状態まで落とし込む。
  関数名・配置場所・引数・戻り値・文言・列順まで指定する。
  「よしなに」「適切に」という言葉が残っていたら、まだ設計が終わっていない。
-->

## 1. 背景・目的

<なぜこの機能が必要か、どんな課題を解決するか。
 既存の類似機能との違いが説明できると、実装AIが誤って既存機能を改変するのを防げる。>

---

## 2. 変更対象ファイル ＋ 機能要件

| ファイル | 変更内容 |
|---|---|
| `src/<file1>` | 新関数 `<name>()` を追加 |
| `src/<file2>` | <どこに何を追加するか> |

### `src/<file1>`

- 関数名: `<name>()`
- 配置: `<既存関数名>` の直後
- 引数 / 戻り値: `<型と意味>`
- 処理内容:
  - <ステップを箇条書きで。参照する既存関数名も明示する>
  - <定数・ラベル・列順など、値が決まっているものは全て書き下す>

### `src/<file2>`

- 挿入位置: `<セレクタや既存要素名で一意に特定できる形で>`
- 追加するコード:

```html
<!-- そのまま貼れる粒度で書く -->
```

---

## 3. UIフロー（操作シーケンス）

1. ユーザーが <画面/タブ> を開く
2. <要素> が表示される
3. <操作> → <システムの反応>
4. <完了状態>

---

## 4. 完了条件（DoD）

<!-- 「動くこと」ではなく「観測できること」を書く。レビューエージェントがそのまま検証に使う。 -->

- [ ] <ロール/条件> で <要素> が表示される
- [ ] <別のロール/条件> では表示されない
- [ ] <操作> すると <結果> になる
- [ ] <出力の形式・順序・命名が仕様通り>
- [ ] <バックエンドでの防御が効いている（フロントを迂回してもエラーになる）>

---

## 5. 注意事項・禁止事項

- **スコープ外変更禁止**: <触ってはいけない既存関数・ファイルを名指しする>
- **<アーキテクチャ規約>**: <破ってはいけないルールを再掲>
- **<やらないことの明示>**: <「確認ダイアログは設けない」など、実装AIが気を利かせて追加しがちなものを先に潰す>
