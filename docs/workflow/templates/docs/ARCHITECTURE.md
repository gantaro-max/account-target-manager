# <プロジェクト名> - データ設計

> 本ファイルは Agent-B（データ整合性レビュー）の参照元です。
> ここに書かれていないルールは検証されません。**不変条件は必ずここに明記すること。**

## 1. データ定義

### `<テーブル/シート名>`

| 列名 | プロパティ名 | 内容 |
| :--- | :--- | :--- |
| `<column>` | `<prop>` | <説明> |

## 2. ID 採番規則

| エンティティ | プレフィックス | 生成方法 |
| :--- | :--- | :--- |
| <エンティティ> | `<x>` | `<x>` + <タイムスタンプ等> |

## 3. リレーション

```
<親> (1) ─── (N) <子> (1) ─── (N) <孫>
```

| 子 | 外部キー | 参照先 |
| :--- | :--- | :--- |
| `<child>` | `<parentId>` | `<parent>.id` |

## 4. 不変条件（Agent-B 検証対象）

### 4.1 カスケード削除

| 削除対象 | 連鎖削除されるもの |
| :--- | :--- |
| <親> | <子>・<孫>・<関連履歴> |

### 4.2 永続化方式

<一括送信 / 都度通信 / トランザクション境界など>

### 4.3 履歴・監査

<状態変更時に履歴レコードを残すルール>
