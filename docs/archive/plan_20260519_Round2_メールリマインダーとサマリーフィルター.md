# 実装指示書 - Round 2: 期限超過メール通知 & サマリータブ取引先名フィルター

**作成日**: 2026-05-19
**対象ブランチ**: main
**担当**: Codex

---

## 1. 概要

2件の機能追加を行う。

1. **期限超過メールリマインダー (F)**: GASの時間ドリブントリガーで毎朝実行し、`nextActionDate` が当日以前の案件を担当者へメール通知する（フロントエンド変更なし）
2. **サマリータブ取引先名フィルター (H)**: サマリータブの案件一覧に取引先名フリーワード検索フィールドを追加する

---

## 2. 修正① 期限超過メールリマインダー

### 変更対象: `src/コード_v2_9.js`

ファイル末尾に以下の関数を追加する。

```javascript
// ============================================================
// 期限超過アクション リマインダーメール送信
// ※GASエディタで時間ドリブントリガー（毎日・午前8時台）に設定すること
// ============================================================
function sendOverdueReminders() {
  const tz = Session.getScriptTimeZone();
  const today = Utilities.formatDate(new Date(), tz, "yyyy-MM-dd");

  const users    = _getRows(SHEET.USERS);
  const projects = _getRows(SHEET.PROJECTS);
  const accounts = _getRows(SHEET.ACCOUNTS);

  // 期限超過の案件（クロージング除く）
  const overdue = projects.filter(p =>
    p.next_action_date &&
    String(p.next_action_date).slice(0, 10) < today &&
    String(p.current_stage || "0") !== "4"
  );

  if (overdue.length === 0) return;

  // 担当者名 → 担当者オブジェクト の逆引きマップ
  const nameToUser = {};
  users.forEach(u => { if (u.name) nameToUser[u.name] = u; });

  // 担当者ごとにグループ化
  const byAssignee = {};
  overdue.forEach(p => {
    const key = p.next_action_assignee || "未設定";
    if (!byAssignee[key]) byAssignee[key] = [];
    byAssignee[key].push(p);
  });

  Object.entries(byAssignee).forEach(([assigneeName, items]) => {
    const user = nameToUser[assigneeName];
    if (!user || !user.user_id) return; // メールアドレスが取れない場合はスキップ

    const hospMap = {};
    accounts.forEach(h => { hospMap[h.id] = h.name || h.id; });

    const lines = items.map(p => {
      const hName = hospMap[p.account_id] || p.account_id;
      const dueStr = String(p.next_action_date).slice(0, 10);
      return `・[${hName}] ${p.name}\n  期限: ${dueStr}　内容: ${p.next_action_text || "（未入力）"}`;
    });

    const subject = `【リマインド】期限超過のネクストアクションが ${items.length} 件あります`;
    const body = [
      `${assigneeName} さん`,
      "",
      "以下の案件でネクストアクションの期限が過ぎています。",
      "取引先ターゲット管理アプリより確認・更新をお願いします。",
      "",
      ...lines,
      "",
      "---",
      "このメールはシステムから自動送信されています。",
    ].join("\n");

    MailApp.sendEmail({
      to: user.user_id,
      subject,
      body,
      name: "取引先ターゲット管理システム",
    });
  });
}
```

### トリガー設定（手動作業・コード変更なし）

GASエディタで以下の設定を行う（Codexの作業対象外・ユーザーが実施）:
- 「トリガー」メニュー → 「トリガーを追加」
- 関数: `sendOverdueReminders`
- 実行のタイミング: 時間ドリブン → 日タイマー → 午前8時〜9時

---

## 3. 修正② サマリータブ 取引先名フィルター

### 3-1. `src/js_core.html` — 状態変数の追加

`currentStageFilter` の宣言がある `src/js.html` に既にある。
代わりに `js_core.html` の状態変数ブロック（`let editingDeptId = null;` 付近）に1行追加する。

```javascript
// 変更前
  let editingDeptId = null;

// 変更後
  let currentHospNameFilter = "";
  let editingDeptId = null;
```

### 3-2. `src/js.html` — フィルター更新関数の追加

`updateStageFilter()` 関数の直後に追加する。

```javascript
  function updateHospNameFilter(val) {
    currentHospNameFilter = val.trim();
    renderSummary();
  }
```

### 3-3. `src/index.html` — 検索フィールドの追加

`id="stage-filter"` の `<select>` 要素の直後に以下を追加する。

```html
              <input
                type="text"
                id="hosp-name-filter"
                placeholder="取引先名で絞り込み"
                oninput="updateHospNameFilter(this.value)"
                style="padding:4px 8px; font-size:12px; border:1px solid var(--border); border-radius:4px; width:140px;"
              />
```

### 3-4. `src/js_review.html` — `renderSummary()` へフィルター適用

`renderSummary()` 内の `let filteredProjs = ...` ブロックを以下のように変更する。

```javascript
// 変更前
    let filteredProjs = db.projects.filter((p) =>
      hospIds.includes(p.accountId),
    );
    if (currentStageFilter !== "all") {
      filteredProjs = filteredProjs.filter(
        (p) => String(p.currentStage) === String(currentStageFilter),
      );
    }

// 変更後
    let filteredProjs = db.projects.filter((p) =>
      hospIds.includes(p.accountId),
    );
    if (currentStageFilter !== "all") {
      filteredProjs = filteredProjs.filter(
        (p) => String(p.currentStage) === String(currentStageFilter),
      );
    }
    if (currentHospNameFilter) {
      const kw = currentHospNameFilter.toLowerCase();
      filteredProjs = filteredProjs.filter((p) => {
        const h = db.accounts.find((h) => h.id === p.accountId);
        return h && h.name.toLowerCase().includes(kw);
      });
    }
```

---

## 4. 完了条件 (Definition of Done)

- [ ] `sendOverdueReminders()` が `コード_v2_9.js` に追加されている
- [ ] 期限超過案件がない場合は何もしない（早期リターン）
- [ ] `user_id` が取得できない担当者はスキップ（エラーにならない）
- [ ] サマリータブに「取引先名で絞り込み」テキスト入力が表示される
- [ ] テキスト入力が空の場合はすべての取引先が表示される（フィルターなし）
- [ ] 入力した文字列（大文字小文字無視）で取引先名が部分一致フィルターされる
- [ ] ステージフィルターと取引先名フィルターを同時に使用できる（AND条件）
- [ ] `node --check` で構文エラーなし（GAS側はGASエディタで構文確認）
