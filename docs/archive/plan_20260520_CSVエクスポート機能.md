# 実装指示書：CSV エクスポート機能

作成日: 2026-05-20

---

## 概要

「📝 活動＆アクション」タブ上部に **活動ログ** と **案件一覧** をCSV出力するボタンを追加する。
GAS 側でロールに応じたスコープのデータを CSV 文字列（BOM 付き UTF-8）として返し、
フロントエンドで Blob ダウンロードする。

- `member` / `leader` ロール：自分の担当取引先のデータのみ
- `manager` / `admin` ロール：全担当者のデータ

---

## 変更対象ファイル

1. `src/コード_v2_9.js`
2. `src/index.html`
3. `src/js_log.html`
4. `docs/TASKS.md`

---

## 1. `src/コード_v2_9.js` の変更

### 1-1. `_buildCsv()` ヘルパーを追加

`getStageHistory` 関数の直前（line 810 付近）に追記する。

```javascript
function _buildCsv(headers, rows) {
  const esc = (v) => {
    const s = String(v == null ? "" : v);
    return s.includes(",") || s.includes("\n") || s.includes('"')
      ? '"' + s.replace(/"/g, '""') + '"'
      : s;
  };
  return "﻿" + [headers, ...rows].map((r) => r.map(esc).join(",")).join("\r\n");
}
```

### 1-2. `getCsvData(type)` を追加

`_buildCsv` の直後に追記する。

```javascript
function getCsvData(type) {
  const email = Session.getActiveUser().getEmail();
  const role = _getMyRole();
  const isAll = role === "admin" || role === "manager";
  const tz = Session.getScriptTimeZone();

  const accounts = _getRows(SHEET.ACCOUNTS);
  const myHospIds = new Set(
    accounts
      .filter((h) => isAll || h.assigned_user_id === email)
      .map((h) => h.id),
  );

  const users = _getRows(SHEET.USERS);
  const userNameMap = {};
  users.forEach((u) => {
    userNameMap[u.user_id] = u.name || u.user_id;
  });

  const hospMap = {};
  accounts.forEach((h) => {
    hospMap[h.id] = { name: h.name, userId: h.assigned_user_id };
  });

  if (type === "logs") {
    const projects = _getRows(SHEET.PROJECTS);
    const departments = _getRows(SHEET.DEPARTMENTS);
    const logs = _getRows(SHEET.LOGS);

    const projMap = {};
    projects.forEach((p) => {
      projMap[p.id] = { name: p.name, deptId: p.department_id };
    });
    const deptMap = {};
    departments.forEach((d) => {
      deptMap[d.id] = d.name;
    });

    const headers = [
      "担当者名", "取引先名", "部署名", "案件名",
      "訪問日", "活動内容", "同行者", "添付URL", "更新日時",
    ];
    const rows = logs
      .filter((l) => myHospIds.has(l.account_id))
      .map((l) => {
        const proj = projMap[l.project_id] || {};
        const hosp = hospMap[l.account_id] || {};
        return [
          userNameMap[hosp.userId] || hosp.userId || "",
          hosp.name || "",
          deptMap[proj.deptId] || "",
          proj.name || "",
          l.visit_date || "",
          l.content || "",
          l.accompanied_by || "",
          l.estimate_url || "",
          l.updated_at
            ? Utilities.formatDate(new Date(l.updated_at), tz, "yyyy-MM-dd HH:mm")
            : "",
        ];
      });

    return _buildCsv(headers, rows);
  }

  if (type === "projects") {
    const STAGE_LABELS_CSV = [
      "0：未接触", "1：初回訪問済", "2：ニーズ確認中", "3：提案実施中", "4：クロージング",
    ];
    const projects = _getRows(SHEET.PROJECTS);
    const departments = _getRows(SHEET.DEPARTMENTS);
    const deptMap = {};
    departments.forEach((d) => {
      deptMap[d.id] = d.name;
    });

    const headers = [
      "担当者名", "取引先名", "部署名", "案件名", "対象製品",
      "進捗ステージ", "最新課題メモ", "次回予定日",
      "ネクストアクション", "アクション担当者", "受注/失注",
    ];
    const rows = projects
      .filter((p) => myHospIds.has(p.account_id))
      .map((p) => {
        const hosp = hospMap[p.account_id] || {};
        const stageNum = parseInt(p.current_stage) || 0;
        return [
          userNameMap[hosp.userId] || hosp.userId || "",
          hosp.name || "",
          deptMap[p.department_id] || "",
          p.name || "",
          p.target_product || "",
          STAGE_LABELS_CSV[stageNum] || String(stageNum),
          p.latest_note || "",
          p.next_action_date || "",
          p.next_action_text || "",
          p.next_action_assignee || "",
          p.closing_status || "",
        ];
      });

    return _buildCsv(headers, rows);
  }

  throw new Error("不正なtype: " + type);
}
```

---

## 2. `src/index.html` の変更

`tab0` 内の `<div class="control-bar">` の**内側**、`#t0-account` セレクターの直後（`</div>` の前）にボタンを追記する。
`margin-left:auto` で右端に押し込む（control-bar は `display:flex` のため有効）。

```html
// Before:
        <select
          class="hosp-select"
          id="t0-account"
        ></select>
      </div>

// After:
        <select
          class="hosp-select"
          id="t0-account"
        ></select>
        <div style="margin-left:auto; display:flex; gap:6px;">
          <button class="control-btn" onclick="downloadCsv('logs')">📥 活動ログ CSV</button>
          <button class="control-btn" onclick="downloadCsv('projects')">📥 案件 CSV</button>
        </div>
      </div>
```

---

## 3. `src/js_log.html` の変更

ファイル末尾の `</script>` タグの直前に `downloadCsv` 関数を追記する。

```javascript
function downloadCsv(type) {
  Swal.fire({
    title: "CSV出力中...",
    allowOutsideClick: false,
    didOpen: () => Swal.showLoading(),
  });
  google.script.run
    .withSuccessHandler((csv) => {
      Swal.close();
      const blob = new Blob([csv], { type: "text/csv;charset=utf-8;" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download =
        type === "logs"
          ? `活動ログ_${new Date().toISOString().slice(0, 10)}.csv`
          : `案件一覧_${new Date().toISOString().slice(0, 10)}.csv`;
      document.body.appendChild(a);
      a.click();
      setTimeout(() => {
        document.body.removeChild(a);
        URL.revokeObjectURL(url);
      }, 100);
    })
    .withFailureHandler((e) => {
      Swal.fire("エラー", e.message, "error");
    })
    .getCsvData(type);
}
```

---

## 4. `docs/TASKS.md` の変更

「CSVデータエクスポート機能」を `[x]` に更新し、内容を補足する。

```markdown
// Before:
- [ ] **CSVデータエクスポート機能**
  - 登録された活動ログや案件状況を、二次利用のためにCSV形式でダウンロードできる機能。

// After:
- [x] **CSVデータエクスポート機能**
  - 登録された活動ログや案件状況を、二次利用のためにCSV形式でダウンロードできる機能。
  - 「活動＆アクション」タブ上部に「📥 活動ログ CSV」「📥 案件 CSV」ボタンを配置。
  - member/leader は自担当分のみ、manager/admin は全担当者分を出力。
  - BOM付きUTF-8のため Excel で文字化けなし。担当者名・取引先名・部署名を結合して出力。
  - 実装指示書: `docs/archive/campaign_20260520_CSVエクスポート機能.md`
```

---

## DoD（完了条件）チェックリスト

- [ ] 1. `_buildCsv()` ヘルパーを `コード_v2_9.js` に追加
- [ ] 2. `getCsvData("logs")` の実装（ロールスコープ + 担当者名/取引先名/部署名/案件名の結合）
- [ ] 3. `getCsvData("projects")` の実装（ステージラベル変換含む）
- [ ] 4. `index.html` の `tab0` にエクスポートボタン2個を追加
- [ ] 5. `js_log.html` に `downloadCsv(type)` 関数を追加（SweetAlert2 ローディング付き）
- [ ] 6. `docs/TASKS.md` の該当タスクを `[x]` に更新

---

## 注意事項

- `_buildCsv` はカンマ・改行・ダブルクォートを含むフィールドを RFC 4180 に従いクォートする。
- BOM（`﻿`）を先頭に付与することで、Excel で直接開いた際の文字化けを防ぐ。
- `活動ログ.updated_at` が空の場合は更新日時列を空欄で出力（`updated_at` 追加前のレコード）。
- GAS 側の `getCsvData` は `google.script.run` 経由で呼ぶため、戻り値は文字列（最大 50MB 程度）。件数が膨大になった場合の対応は現時点では不要。
