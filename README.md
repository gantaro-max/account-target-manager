# account-target-manager

**Google Apps Script + スプレッドシートで構築した、階層型のBtoB営業ターゲット管理ツール（SFA/CRM）**

「取引先 → 部署 → 案件 → 活動ログ」の4階層で営業活動を管理し、
ロール別の権限制御・月次レビュー・統計可視化・CSV出力までを1つのWebアプリにまとめています。

インフラを持たず、Googleアカウントだけで動くことを制約条件として設計しました。

---

## 特徴

### インフラゼロで動く

サーバーもDBも不要です。スプレッドシートをDBとして扱い、GASのWebアプリとして公開します。
運用コストはGoogleアカウントの範囲内で完結します。

### GASの実行時間制限を前提にした同期設計

GASには1実行あたり6分の制限があり、`google.script.run` の往復も安くありません。
そのため**フロントエンドのメモリ上で編集を積み上げ、最後に一括Upsertで同期する**方式を採っています。
操作のたびにサーバーへ書きに行く設計では、実用的な速度になりませんでした。

### 階層データの整合性をアプリ側で担保

スプレッドシートには外部キー制約もカスケード削除もありません。
そのため以下をアプリケーション層の不変条件として実装しています。

- **ID採番規則**: 取引先 `a`／部署 `d`／案件 `p`／ログ `l` + timestamp
- **カスケード削除**: 取引先を消せば、配下の部署・案件・活動ログ・ステージ履歴まで連鎖削除
- **ステージ変更履歴**: 案件の進捗ステージが変わるたびに `stage_history` へ追記

### 4段階のロールベースアクセス制御

| ロール | 権限 |
| :--- | :--- |
| `admin` | 全権限。全ユーザーのデータ閲覧・編集、取引先削除、設問管理 |
| `manager` | 全取引先の閲覧（読み取り専用）。設問管理・月次評価コメントの編集 |
| `leader` | 担当取引先の閲覧・編集 |
| `member` | 担当取引先の閲覧・編集のみ |

権限はUIの出し分けだけでなく、**GAS側でも `_requireManager()` 等により二重に防御**しています。
フロントエンドを迂回して関数を直接呼んでも通りません。

---

## 主な機能

| 機能 | 概要 |
| :--- | :--- |
| ターゲット管理 | 取引先・部署・案件の階層CRUD。マスターからの検索補完、重複登録の防止 |
| 案件ステージ管理 | 0（未接触）〜4（クロージング）の5段階。期限超過の視覚的アラート |
| 活動ログ | 訪問日・同行者・内容の記録。Googleドライブへのファイル添付 |
| クイック登録 | 取引先 → 部署 → 案件を1フローで連続登録するウィザード |
| 月次レビュー | KPI自動集計（接点部署数・同行回数・提案中案件数）とセルフチェック |
| 統計 | Chart.js によるステージ別・受注失注・月別活動・担当者別のグラフ |
| キャンペーン | 期間限定の重点取組を案件に紐付け。期間内のもののみ選択肢に表示 |
| CSV出力 | 取引先・案件・期間の3フィルターを組み合わせた18列統一フォーマット |
| メールリマインダー | 時間トリガーで期限超過の案件を担当者へ自動通知 |

---

## 技術スタック

| 領域 | 使用技術 |
| :--- | :--- |
| Frontend | HTML5 / CSS3 / Vanilla JavaScript（ビルドツールなし） |
| Backend | Google Apps Script（Web App / V8ランタイム） |
| Database | Google スプレッドシート（10シート） |
| Storage | Google ドライブ（添付ファイル） |
| ライブラリ | [Tom Select v2](https://tom-select.js.org/)（検索可能セレクト）/ [SweetAlert2 v11](https://sweetalert2.github.io/)（ダイアログ）/ [Chart.js v4](https://www.chartjs.org/)（グラフ） |

規模: 約 7,400 行 / 13 ファイル

## ファイル構成

| ファイル | 役割 |
| :--- | :--- |
| `src/コード.js` | サーバーサイド。スプレッドシート操作・権限判定・ファイル管理・メール通知 |
| `src/index.html` | UIスケルトン（タブ・モーダル・CDN読み込み） |
| `src/css.html` | レスポンシブなスタイル定義 |
| `src/js_core.html` | 状態管理と共通UIユーティリティ |
| `src/js.html` | 初期化・タブ制御・取引先セレクト |
| `src/js_account.html` | 取引先のCRUD・マスター参照 |
| `src/js_department.html` | 部署のCRUD・カスケード削除 |
| `src/js_project.html` | 案件のCRUD・ステージ管理 |
| `src/js_log.html` | 活動ログ・ファイル添付・CSV出力 |
| `src/js_review.html` | 月次レビュー・設問管理・キャンペーン管理 |
| `src/js_quick_add.html` | クイック登録ウィザード |
| `src/js_stats.html` | 統計グラフ描画 |

> 当初は単一の `js.html` に全ロジックが集中していたため、機能追加のタイミングで
> ドメインごとにモジュール分割するリファクタリングを行いました。

---

## セットアップ

[docs/SETUP.md](docs/SETUP.md) を参照してください。
スプレッドシートの準備、スクリプトプロパティの設定、デプロイ手順を記載しています。

## ドキュメント

| ドキュメント | 内容 |
| :--- | :--- |
| [PROJECT_OVERVIEW.md](docs/PROJECT_OVERVIEW.md) | システム全体像・技術スタック |
| [REQUIREMENTS.md](docs/REQUIREMENTS.md) | 機能要件・非機能要件・権限体系 |
| [DATABASE_DESIGN.md](docs/DATABASE_DESIGN.md) | シート定義・リレーション・カスケード削除ルール |
| [UI_FLOW.md](docs/UI_FLOW.md) | 画面構成・タブ遷移 |
| [MANUAL_MEMBER.md](docs/MANUAL_MEMBER.md) | 担当者向け操作説明書 |
| [MANUAL_MANAGER.md](docs/MANUAL_MANAGER.md) | 管理者向け操作説明書 |
| [TASKS.md](docs/TASKS.md) | 開発タスク・既知の制限事項 |
| `docs/archive/` | 実装指示書のアーカイブ（36本） |

## 開発プロセスについて

本プロジェクトは、設計と実装をAIエージェントで分担する開発フローで構築しました。
その方法論は別リポジトリ [plan-driven-agents](https://github.com/gantaro-max/plan-driven-agents) に切り出しています。
`docs/archive/` にある指示書は、そのフローで実際に使用したものです。

## ライセンス

[MIT](LICENSE)
